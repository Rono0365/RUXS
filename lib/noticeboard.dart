import 'package:flutter/material.dart';
class transitprime extends StatelessWidget {
  const transitprime({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}